import React from 'react'

const BlogNav = ({ iconfill, data }) => {
    return (
        <div className="blog-nav">
            <nav>
                <a href="/">Design Resources</a>
                <a href="/">Articles</a>
            </nav>
        </div>
    );
}

export default BlogNav